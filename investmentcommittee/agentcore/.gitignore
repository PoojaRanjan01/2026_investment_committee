# Secrets (local environment files are never committed)
.env.local

# Deploy target (account ID) — copy aws-targets.json.example and fill in your own
aws-targets.json

# CDK Build Artifacts
cdk/cdk.out/
cdk/node_modules/

# CLI Internals
.cli/*

# Ephemeral Staging
.cache/*

# Exception: Commit the State
!.cli/deployed-state.json
